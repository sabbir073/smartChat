node_modules/
.pnpm-store/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo
.turbo/
.env
.env.*
!.env.example
*.log
npm-debug.log*
pnpm-debug.log*
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
test-results/
playwright-report/
blob-report/
.playwright/
tmp/
uploads/
backups/

# local verification scratch
verify.log
install.log
install.err.log

# prisma CLI host connection
packages/database/.env
.tools/
msg.txt

# Certificates and the ACME webroot. Live TLS private keys, written by certbot into the
# working tree on a deployed host; `git add -A` there must never be able to pick them up.
infrastructure/nginx/certs/
infrastructure/nginx/acme/
